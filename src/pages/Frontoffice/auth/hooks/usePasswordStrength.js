/**
 * usePasswordStrength – re-export from profile module for auth flow reuse.
 * Single source of truth for password evaluation.
 */
export { default } from '../../profile/hooks/usePasswordStrength';
